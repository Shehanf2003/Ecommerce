// backend/__tests__/server.test.js

describe('Sanity Check', () => {
    test('should pass this basic test to confirm Jest is working', () => {
        expect(true).toBe(true);
    });

    test('should calculate 1 + 1 correctly', () => {
        expect(1 + 1).toBe(2);
    });
});